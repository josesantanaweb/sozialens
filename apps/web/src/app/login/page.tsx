import React from "react";
import Login from "@/components/Auth/Login";

export const metadata = {
  title: "Login | Sozialens",
  description: "Login to Sozialens",
};

const LoginPage = (): JSX.Element => <Login />

export default LoginPage;
